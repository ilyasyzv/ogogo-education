/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["utfs.io", "encrypted-tbn0.gstatic.com", "www.freecodecamp.org", "example.com"],
  },
};

module.exports = nextConfig;
